# DSH packages are declared as optional peers so a host install can dedupe them,
# but a standalone clone must still resolve them to typecheck and build.
auto-install-peers=true
strict-peer-dependencies=false
